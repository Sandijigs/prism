# CRE Workflow Settings for threshold-controller

local-simulation:
  user-workflow:
    workflow-name: "threshold-controller"
  workflow-artifacts:
    workflow-path: "./index.ts"
    config-path: "./config.json"
